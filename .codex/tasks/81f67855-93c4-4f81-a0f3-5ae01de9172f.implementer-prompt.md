Implement the approved PlanFlow location false-positive fix. You may edit only:
- lib/services/voice_schedule_structure_service.dart
- lib/services/voice_command_analysis_service.dart
- lib/services/location_lookup_service.dart
- test/services/voice_schedule_structure_service_test.dart
- test/services/voice_command_analysis_service_test.dart
- test/services/location_lookup_service_test.dart

Do not edit UI, DB, migrations, settings, AGENTS, CLAUDE, pubspec, or scripts. Preserve unrelated dirty changes.

Requirements:
1. `매주 목요일 오후 3시 태블릿계기판 중요한일정으로 저장` keeps recurrence/time/is_critical/title but produces null location and no coordinates.
2. A noun alone cannot auto-confirm a location. Reject candidates containing schedule metadata/commands including important schedule, repeat, save/create, alarm/reminder, and date/time, unless an independently extracted explicit place span remains.
3. Preserve valid explicit location spans, e.g. `강남역에서 중요한 일정` resolves only `강남역`.
4. Expose reusable normalized map-result similarity and a location confidence evaluation usable by callers. Similarity >= .51 can add evidence but cannot by itself auto-confirm a weak noun.
5. Apply thresholds: >= .75 automatic resolution eligible, .45-.74 confirmation-only eligible (no coordinate auto-confirm), < .45 blank location. Reuse existing candidate confirmation mechanics; do not add UI.
6. Add focused unit tests for reported phrase, explicit-span preservation, similarity .51 boundary, score thresholds, and existing valid place behavior.

Run dart format on only changed Dart files and targeted tests. Do not self-approve; report exact changed files, tests, and unresolved risks.
