Do not call tools. Produce a concise Explorer report based only on these verified local facts:
1. `VoiceCommandAnalysisService._normalizeScheduleFields` sends model-provided `schedule_fields.location` through `VoiceScheduleStructureService.normalizeScheduleLocation`.
2. `normalizeScheduleLocation` accepts a non-empty explicit candidate after `_normalizeLocationCandidate`, and `_isInvalidLocationCandidate` rejects only date/time metadata and a few verbal fragments.
3. Current rejection does not reject `태블릿계기판 중요한일정` or strings containing the importance command.
4. `LocationLookupService` has relevance sorting but no public normalized similarity threshold for automatic coordinate resolution.
5. Existing tests prove time-only rejection and explicit place extraction but do not cover the provided phrase.

Return only: goal, root cause, affected symbols, minimal implementation surface, and focused regression tests. No file edits.
