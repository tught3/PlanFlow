Do not call tools. Plan the following verified implementation.

Explorer facts:
- VoiceCommandAnalysisService._normalizeScheduleFields forwards model-provided location into VoiceScheduleStructureService.normalizeScheduleLocation.
- normalizeScheduleLocation accepts a non-empty explicit candidate through _normalizeLocationCandidate.
- _isInvalidLocationCandidate does not reject `태블릿계기판 중요한일정`.
- LocationLookupService has private relevance scoring and a public `sortByRelevance`, but no explicit public normalized similarity cutoff.

Product decisions:
- A noun alone is not enough to auto-confirm a place.
- Reject candidates containing schedule commands/metadata (`중요 일정`, repeat/save/alarm commands, date/time) unless an independently extracted explicit place span remains.
- Use scored candidates: explicit particle or place-type suffix and movement context are positive evidence; map result normalized similarity >= 0.51 and history match may add evidence.
- Score >= .75 permits automatic coordinate resolution; .45-.74 is shown as ranked confirmation; below .45 leaves location blank and preserves title.
- No database migration.

Return a compact, decision-complete plan: items classified Simple/Complex, allowed files, forbidden files, exact tests and completion gates. Do not edit.
