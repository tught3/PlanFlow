class AlarmService {
  const AlarmService();

  // TODO: Wire Android alarm handling for briefing jobs.
  Future<void> scheduleBriefing({
    required String id,
    required DateTime scheduledAt,
  }) =>
      throw UnimplementedError('AlarmService.scheduleBriefing');
}
