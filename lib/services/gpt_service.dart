class GptService {
  const GptService();

  // TODO: Implement GPT parsing and schedule normalization in a later checklist.
  Future<Map<String, dynamic>> parseSchedule(String rawText) =>
      throw UnimplementedError('GptService.parseSchedule');
}
