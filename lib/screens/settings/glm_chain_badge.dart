import 'package:flutter/material.dart';

class GlmChainBadge extends StatelessWidget {
  const GlmChainBadge({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('GLM Chain V1');
  }
}
